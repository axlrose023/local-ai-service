# Local Corporate AI Assistant — Roadmap разработки

> Последовательный план от нуля до production-ready системы.

---

## Обзор этапов

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                             │
│  ЭТАП 1          ЭТАП 2          ЭТАП 3          ЭТАП 4          ЭТАП 5    │
│  Инфра           RAG             UI              Hardening       Scale     │
│  ──────          ───             ──              ─────────       ─────     │
│  День 1          День 2          День 3          Неделя 2        Месяц 2+  │
│                                                                             │
│  ┌─────┐        ┌─────┐        ┌─────┐        ┌─────────┐      ┌────────┐  │
│  │vLLM │        │Ingest│       │Chain-│       │ Auth    │      │ ACL    │  │
│  │Chroma│  ───► │Search│  ───► │ lit  │  ───► │ Logging │ ───► │ Hybrid │  │
│  │Docker│       │RAG   │       │Chat  │       │ Backup  │      │ Monitor│  │
│  └─────┘        └─────┘        └─────┘        └─────────┘      └────────┘  │
│                                                                             │
│  ════════════════════════════════════════════════════════════════════════  │
│                              MVP (3 дня)                    Production     │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## Этап 1: Инфраструктура

**Цель:** Поднять базовые сервисы, убедиться что LLM отвечает.

**Время:** 1 день

### Задачи

| # | Задача | Критерий готовности |
|---|--------|---------------------|
| 1.1 | Создать структуру проекта | Папки и файлы созданы |
| 1.2 | Написать docker-compose.yml | `docker-compose config` без ошибок |
| 1.3 | Поднять ChromaDB | `curl localhost:8001/api/v1/heartbeat` → OK |
| 1.4 | Поднять vLLM/Ollama | Модель загружена, health OK |
| 1.5 | Проверить LLM | curl запрос возвращает ответ |

### Детали

**1.1 Структура проекта**

```bash
mkdir -p local-ai-assistant/{docs,chroma_data,.chainlit}
cd local-ai-assistant
touch docker-compose.yml Dockerfile requirements.txt .env
touch app.py config.py ingest.py rag.py llm.py
```

**1.2 docker-compose.yml**

Минимальная версия для проверки:

```yaml
version: "3.9"

services:
  chromadb:
    image: chromadb/chroma:0.4.24
    ports:
      - "8001:8000"
    volumes:
      - ./chroma_data:/chroma/chroma

  inference:
    image: ollama/ollama:latest  # Начинаем с Ollama — проще
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]

volumes:
  ollama_data:
```

**1.5 Проверка LLM**

```bash
# Скачиваем модель (Ollama)
docker-compose exec inference ollama pull qwen2.5:7b  # Сначала маленькую

# Тестируем
curl http://localhost:11434/api/generate -d '{
  "model": "qwen2.5:7b",
  "prompt": "Привет! Как дела?",
  "stream": false
}'
```

### Результат этапа

- [ ] ChromaDB работает
- [ ] LLM отвечает на запросы
- [ ] GPU используется (nvidia-smi показывает загрузку)

---

## Этап 2: RAG Pipeline

**Цель:** Научить систему искать информацию в документах.

**Время:** 1 день

### Задачи

| # | Задача | Критерий готовности |
|---|--------|---------------------|
| 2.1 | Написать config.py | Настройки загружаются из .env |
| 2.2 | Написать ingest.py | PDF индексируется в ChromaDB |
| 2.3 | Написать rag.py | Поиск возвращает релевантные чанки |
| 2.4 | Написать llm.py | Стриминг ответов работает |
| 2.5 | Интеграционный тест | Вопрос → Поиск → Ответ с контекстом |

### Детали

**2.2 Тест ingest.py**

```bash
# Кладём тестовый PDF
cp ~/some_document.pdf ./docs/

# Запускаем индексацию
python ingest.py

# Проверяем что документы в базе
python -c "
import chromadb
client = chromadb.HttpClient(host='localhost', port=8001)
col = client.get_collection('corporate_docs')
print(f'Documents: {col.count()}')
"
```

**2.3 Тест rag.py**

```python
# test_rag.py
from rag import search_documents, format_context

results = search_documents("как настроить VPN")
print(f"Найдено: {len(results)} документов")
for r in results:
    print(f"- {r['source']}: {r['content'][:100]}...")
```

**2.5 Интеграционный тест**

```python
# test_integration.py
import asyncio
from rag import search_documents, format_context
from llm import chat_stream

async def test():
    query = "Как настроить VPN?"

    # RAG
    docs = search_documents(query)
    context = format_context(docs)
    print(f"Контекст: {len(context)} символов")

    # LLM
    print("\nОтвет:")
    async for token in chat_stream(query, context):
        print(token, end="", flush=True)
    print()

asyncio.run(test())
```

### Результат этапа

- [ ] Документы индексируются
- [ ] Поиск находит релевантные фрагменты
- [ ] LLM генерирует ответ на основе контекста

---

## Этап 3: UI (Chainlit)

**Цель:** Пользовательский интерфейс для чата.

**Время:** 1 день

### Задачи

| # | Задача | Критерий готовности |
|---|--------|---------------------|
| 3.1 | Написать app.py | Chainlit запускается |
| 3.2 | Интеграция с RAG | Поиск отображается как Step |
| 3.3 | Стриминг ответов | Токены появляются постепенно |
| 3.4 | Отображение источников | Источники видны под ответом |
| 3.5 | Dockerfile для app | Контейнер собирается и работает |

### Детали

**3.1 Локальный запуск**

```bash
# Устанавливаем зависимости
pip install -r requirements.txt

# Запускаем Chainlit
chainlit run app.py --port 8000

# Открываем http://localhost:8000
```

**3.2 Проверка Steps**

В UI должен отображаться блок "Поиск в базе знаний" с информацией о найденных документах.

**3.5 Полный запуск в Docker**

```bash
# Обновляем docker-compose.yml (добавляем app сервис)
# Пересобираем
docker-compose up -d --build

# Проверяем логи
docker-compose logs -f app
```

### Результат этапа

- [ ] UI открывается в браузере
- [ ] Можно задать вопрос
- [ ] Ответ стримится с источниками
- [ ] Всё работает в Docker

---

## Этап 4: Hardening (Укрепление)

**Цель:** Подготовить систему к реальному использованию.

**Время:** 1 неделя

### Задачи

| # | Задача | Приоритет | Критерий готовности |
|---|--------|-----------|---------------------|
| 4.1 | Health endpoints | P0 | /health возвращает статус всех сервисов |
| 4.2 | Graceful shutdown | P0 | Ctrl+C корректно останавливает сервисы |
| 4.3 | Базовый auth | P1 | Пароль для входа в UI |
| 4.4 | Логирование | P1 | Структурированные логи в файл |
| 4.5 | Обработка ошибок | P1 | UI показывает понятные сообщения об ошибках |
| 4.6 | Backup скрипт | P2 | chroma_data бэкапится по крону |
| 4.7 | Инкрементальный ingest | P2 | Повторный запуск не дублирует документы |
| 4.8 | Мониторинг GPU | P2 | Алерт при >90% VRAM |

### Детали

**4.1 Health endpoint**

```python
# В app.py добавляем
from fastapi import FastAPI
from fastapi.responses import JSONResponse

app = FastAPI()

@app.get("/health")
async def health():
    checks = {
        "chromadb": await check_chroma(),
        "llm": await check_llm(),
    }
    status = "healthy" if all(checks.values()) else "unhealthy"
    return JSONResponse(
        content={"status": status, "checks": checks},
        status_code=200 if status == "healthy" else 503
    )
```

**4.3 Chainlit Auth**

```python
# В app.py
import chainlit as cl

@cl.password_auth_callback
def auth_callback(username: str, password: str):
    if username == "admin" and password == "secret":  # Из env!
        return cl.User(identifier=username)
    return None
```

**4.6 Backup скрипт**

```bash
#!/bin/bash
# scripts/backup.sh

BACKUP_DIR="/backups/ai-assistant"
DATE=$(date +%Y%m%d_%H%M%S)

mkdir -p $BACKUP_DIR

# Backup ChromaDB
tar -czf "$BACKUP_DIR/chroma_$DATE.tar.gz" ./chroma_data

# Backup docs
tar -czf "$BACKUP_DIR/docs_$DATE.tar.gz" ./docs

# Удаляем старые бэкапы (>7 дней)
find $BACKUP_DIR -mtime +7 -delete

echo "Backup completed: $DATE"
```

### Результат этапа

- [ ] Система стабильно работает без падений
- [ ] Есть базовая защита (пароль)
- [ ] Логи пишутся и ротируются
- [ ] Данные бэкапятся

---

## Этап 5: Production Features

**Цель:** Enterprise-функции по мере необходимости.

**Время:** Ongoing (по приоритетам)

### Приоритет P0 (Первый месяц)

| # | Задача | Описание |
|---|--------|----------|
| 5.1 | Улучшение качества RAG | Эксперименты с chunk_size, overlap |
| 5.2 | Больше форматов | XLSX, PPTX, сканы (OCR) |
| 5.3 | История диалогов | Сохранение в Redis/SQLite |
| 5.4 | Фидбек от пользователей | Кнопки 👍/👎, сбор в базу |

### Приоритет P1 (Второй месяц)

| # | Задача | Описание |
|---|--------|----------|
| 5.5 | SSO/LDAP | Интеграция с AD |
| 5.6 | ACL на документах | Метаданные department/access_level |
| 5.7 | Prometheus + Grafana | Базовые метрики |
| 5.8 | Rate limiting | Защита от злоупотреблений |

### Приоритет P2 (Третий месяц+)

| # | Задача | Описание |
|---|--------|----------|
| 5.9 | Hybrid Search | BM25 + Vector |
| 5.10 | Reranker | BGE-reranker для точности |
| 5.11 | Multi-GPU | Масштабирование vLLM |
| 5.12 | Audit Log | Полный лог всех действий |

---

## Чеклист готовности

### MVP (Этапы 1-3)

```
[ ] Docker Compose запускается без ошибок
[ ] ChromaDB healthcheck проходит
[ ] LLM healthcheck проходит
[ ] Документы индексируются (ingest.py)
[ ] Поиск находит релевантные фрагменты
[ ] UI открывается в браузере
[ ] Можно задать вопрос и получить ответ
[ ] Ответ содержит информацию из документов
[ ] Источники отображаются
```

### Production-Ready (Этапы 1-4)

```
[ ] Всё из MVP
[ ] /health endpoint работает
[ ] Базовая аутентификация
[ ] Логи пишутся в файл
[ ] Ошибки обрабатываются gracefully
[ ] Backup скрипт настроен
[ ] Нет memory leaks (работает >24ч)
[ ] Документация для админов
```

### Enterprise (Этап 5)

```
[ ] Всё из Production-Ready
[ ] SSO интеграция
[ ] ACL фильтрация
[ ] Мониторинг и алерты
[ ] Audit logging
[ ] SLA >99%
```

---

## Метрики успеха

### Технические

| Метрика | MVP | Production | Enterprise |
|---------|-----|------------|------------|
| Время ответа (P95) | <30s | <15s | <10s |
| Uptime | 90% | 99% | 99.9% |
| Одновременных пользователей | 1-2 | 10 | 50+ |
| Документов в базе | 100 | 1000 | 10000+ |

### Бизнес

| Метрика | Как измерять |
|---------|--------------|
| Adoption | % сотрудников, использующих систему |
| Satisfaction | Соотношение 👍/👎 |
| Time saved | Опросы пользователей |
| Query success rate | % вопросов с полезным ответом |

---

## Риски и митигация

| Риск | Вероятность | Влияние | Митигация |
|------|-------------|---------|-----------|
| OOM на GPU | Высокая | Высокое | Мониторинг, уменьшение контекста |
| Плохое качество ответов | Средняя | Высокое | Итерации промптов, фидбек |
| Медленная индексация | Средняя | Среднее | Инкрементальный ingest |
| Утечка данных | Низкая | Критичное | ACL, изоляция сети |
| Зависимость от GPU | Средняя | Высокое | Fallback на CPU/меньшую модель |

---

## Timeline

```
Неделя 1
├── День 1: Этап 1 (Инфраструктура)
├── День 2: Этап 2 (RAG)
├── День 3: Этап 3 (UI)
├── День 4-5: Тестирование, баг-фиксы
│
Неделя 2
├── Этап 4.1-4.5 (Health, Auth, Logging)
├── Пилот с 2-3 пользователями
├── Сбор фидбека
│
Неделя 3-4
├── Этап 4.6-4.8 (Backup, Monitoring)
├── Исправления по фидбеку
├── Расширение пилота (10 пользователей)
│
Месяц 2+
├── Этап 5 (по приоритетам)
├── Production rollout
```

---

## Следующий шаг

**Сейчас:** Начинаем Этап 1 — создаём файлы проекта и поднимаем инфраструктуру.

```bash
# Готов начать?
mkdir local-ai-assistant && cd local-ai-assistant
```
